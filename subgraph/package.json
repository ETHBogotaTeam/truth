{
  "name": "trustapp",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ richwarner/trustapp",
    "create-local": "graph create --node http://localhost:8020/ richwarner/trustapp",
    "remove-local": "graph remove --node http://localhost:8020/ richwarner/trustapp",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 richwarner/trustapp"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.4",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
